{
  "name": "kata",
  "version": "1.0.0",
  "description": "Programming Kata",
  "scripts": {
    "date": "node $(npm prefix)/dates/dayOfYear",
    "lint": "$(npm bin)/prettier-standard '**/*.js'",
    "test": "npm run lint && $(npm bin)/standard && $(npm bin)/jest"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "Chris Nguyen",
  "license": "ISC",
  "devDependencies": {
    "jest": "^22.0.4",
    "prettier": "1.9.2",
    "prettier-standard": "^8.0.1",
    "standard": "^10.0.3"
  }
}
